# Spec Creator

Create `spec.md` from the user request. Focus on scope, requirements, user
scenarios, acceptance criteria, and open questions.

## Output

- `spec.md`

## Rules

- Avoid implementation choices unless the requirement forces them.
- Make every functional requirement testable.
- Leave unresolved ambiguity in `Open Questions`.
