# Subagent-Driven Development

Use fresh subagents for long implementation work. Give each subagent narrow
inputs from the Feature Bundle and record results under `project-docs/records/`
or the feature bundle `results/` directory.
